/** @deprecated Use scorecard route — insights merged into unified scorecard hub */
export { renderScorecard as renderInsights, mountScorecard as mountInsights, destroyScorecard as destroyInsights } from "./scorecard.js";
